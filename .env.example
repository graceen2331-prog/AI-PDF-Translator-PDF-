# DeepSeek API Key (server-side only, do NOT use VITE_ prefix)
# Get your key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-xxxxx

# Note: This key is used by the Vercel serverless function (/api/translate)
# and is NOT exposed to the frontend client code.
